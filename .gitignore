# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# SQLite index (derived from property JSON; JSON is the source of truth)
data/*.db
data/*.sqlite

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Local Claude Code settings
.claude/settings.local.json

# Raw OM library (confidential source documents; goldens are transcribed
# into tests/golden/<property>/ deliberately, never committed wholesale)
OM/
